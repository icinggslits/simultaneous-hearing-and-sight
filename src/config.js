const Config = {
	// 面板开关按键
	code: 'KeyP',

	// 是否需要按住Ctrl
	ctrlKey: false,

	// 是否需要按住Alt
	altKey: false,

	// 是否需要按住Shift
	shiftKey: true,

	// 测量方块边长
	measuringBlockResizeSize: 15,

	// 默认音量
	defaultVolume: 40,
};

export default Config;