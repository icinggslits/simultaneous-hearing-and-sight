const Config = {
	// 面板开关按键（大写字母表示需要按住Shift，小写表示不需要按住Shift）
	key: 'P',

	// 是否需要按住Ctrl
	ctrlKey: false,

	// 是否需要按住Alt
	altKey: false,

	// 测量方块边长
	measuringBlockResizeSize: 15,

	// 默认音量
	defaultVolume: 40,
};

export default Config;